## 构建产物

xv6.img: 包含引导程序和内核程序，作为第一块硬盘，hd0
fs.img: 包含用户程序和其他文件，作为第二块硬盘, hd1
qemu挂载xv6.img和fs.img两块硬盘，然后加载hd0(xv6.img)的第一个扇区(bootblock)到指定位置(0x7c00)，开始执行后续的流程,
bios会识别末尾的魔数0x55aa，然后将第一个扇区加载到0x7c00，bootblock承担mbr中引导部分的角色，所以末尾也有这个魔数，但是bootblock
没有创建分区表，所以不算是mbr
xv6中采用一个硬盘一个分区的方式，所以每个硬盘不需要分区表，这也就是为什么会有两个img，如果要合并的话，需要用特殊的工具，给disk.img的mbr加上分区表，然后合并xv6.img和fs.img到disk.img中

## 启动流程
```txt
BIOS
  │
  ▼
bootasm.S（汇编）
  │
  │ ① 开启 A20 地址线
  │ ② 加载 GDT
  │ ③ 切换到 32 位保护模式（Protected Mode）
  │ ④ 跳转到 bootmain()
  │
  ▼
bootmain.c（C 语言） ===> bootmain()
  │
  │ ⑤ 从硬盘读取 ELF 格式的内核
  │ ⑥ 跳转到内核入口（entry.S）物理地址
  │
  ▼
entry.S（汇编，内核入口）===> _start
  │
  │ ① 开启 4MB 大页 (PSE)
  │ ② 加载页目录 entrypgdir
  │ ③ 开启分页 (Paging)
  │ ④ 设置栈指针
  │ ⑤ 跳转到 main() (虚拟地址 0x80100000)
  │
  ▼
main.c（内核主程序）===> main()
  │
  │ ⑩ 初始化内存分配器、进程、文件系统...
```

## kernel.ld
在链接脚本中，规划了kernel的内存分布：
- 虚拟地址分配：
```txt
0x80100000  ──┬── .text 开始
              │
0x8010C800    ├── .text 结束 / .rodata 开始
              │
0x8010F000    ├── .rodata 结束 / 页对齐
              │  (ALIGN 0x1000)
0x80110000    ├── .data 开始
              │
0x80111400    ├── .data 结束 / .bss 开始
              │
0x80116400    ├── .bss 结束 (end)
              │
0x80116400  ──┴── 内核结束
```
- 物理地址分配
```txt
0x00100000  ──┬── .text 开始
              │
0x0010C800    ├── .text 结束
              │
0x0010F000    ├── .rodata 开始
              │
0x00112000    ├── .rodata 结束
              │
0x00113000    ├── .data 开始
              │
0x00114400    ├── .data 结束
              │
0x00114400  ──┴── 注意：.bss 在文件中不占空间（物理地址没有）
```
kernel image的VA和PA是一个线性映射的关系：`VA=PA+0x80000000`

## bootasm.S

## bootmain.c
在bootmain()中，下面这段代码
```c
  elf = (struct elfhdr*)0x10000;  // scratch space

  // Read 1st page off disk
  readseg((uchar*)elf, 4096, 0);
```
kernel image存放于第一个扇区，这里是从第一个扇区看是读取4096字节到`0x10000`处，这里只是一个临时存储区，用来存放elf头
```c
  // Load each program segment (ignores ph flags).
  ph = (struct proghdr*)((uchar*)elf + elf->phoff);
  eph = ph + elf->phnum;
  for(; ph < eph; ph++){
    pa = (uchar*)ph->paddr;
    readseg(pa, ph->filesz, ph->off);
    if(ph->memsz > ph->filesz)
      stosb(pa + ph->filesz, 0, ph->memsz - ph->filesz);
  }
```
读取了elf头之后，就知道了program header table，然后将他们加载到对应的物理地址处，然后还需要注意，如果有bss段，还需要清零，并分配空间（磁盘上不存储bss段）
```c
  // Call the entry point from the ELF header.
  // Does not return!
  entry = (void(*)(void))(elf->entry);
  entry();
```
当.text/.data等段加载到对应的地址后，进入到entry中，经过调试发现是在`entry.S:entry`

## entry.S

## main.c
内存管理采用空闲链表的方式(头插法)
```c
struct run {
  struct run *next;
};

struct {
  struct spinlock lock;
  int use_lock;
  struct run *freelist;
} kmem;
```
`kinit1()`初始化低4MB的内存（不包含内核代码，将空闲页加入到空闲链表中），此时无需加锁，只有一个cpu启动
`kinit2()`初始化剩余内存（将空闲页加入到空闲链表中），此时需要加锁，多个cpu已经启动
`kvmalloc()`初始化内核页表，按照`kmap[]`蓝图, `walkpgdir()`是找到虚拟地址对应的页表项，即PTE的地址, `mpapages`将一段物理地址线性映射到一段虚拟地址处（直接映射就是偏移量为0的线性映射）
`seginit()`只初始化`kcode/kdata/ucode/udata`四个段，并且采用平坦模型，所有段的大小都是4GB，只有特权级有区别

## vm.c
虚拟地址 -> 物理地址
虚拟地址高10位是页目录索引，中10位是页表索引，低12位是页内偏移
CR3存储的是页目录表的物理地址
CR3+PDX=>页表物理地址
页表物理地址+PTX=>页物理地址
页物理地址+OFF=>具体物理地址
```txt

                        虚拟地址 0x80100000
                               │
                   ┌───────────┼───────────┐
                   ▼           ▼           ▼
              PDX=0x200   PTX=0x000   OFF=0x000
                   │           │           │
                   ▼           │           │
CR3=0x00000000 → 页目录        │           │
                  PDE[512] ────┘           │
                   │                       │
                   │ 页表地址=0x00000000    │
                   ▼                       │
                 页表                      │
                 PTE[0] ───────────────────┘
                   │
                   │ 物理页地址=0x00000000
                   ▼
              物理地址 = 0x00000000 + 0x000 = 0x00000000
              (PTE_PS 大页模式时实际为 0x00100000)
```

```txt
虚拟地址空间                           物理地址空间
─────────────────────────────────────────────────────────────────
0xFFFFFFFF                             0xFFFFFFFF
    │                                       │
    │  设备内存 (DEVSPACE)                   │  设备内存
    │  0xFE000000 → 0xFE000000 (直接映射)    │
0xFE000000 ──────────────────────────────────── 0xFE000000
    │                                       │
    │  未使用/空洞                            │
    │                                       │
0x80000000 + PHYSTOP ──────────────────────  PHYSTOP (物理内存上限)
    │                                       │
    │  内核数据段 + 自由物理内存                │
    │  data → PHYSTOP                       │
0x80000000 + data ────────────────────────  V2P(data)
    │                                       │
    │  内核代码段 + 只读数据                   │
    │  KERNLINK → data                      │
0x80000000 + KERNLINK ────────────────────  V2P(KERNLINK)
    │                                       │
    │  I/O 空间 (低 1MB)                     │
    │  KERNBASE → KERNBASE+EXTMEM           │
0x80000000 (KERNBASE) ────────────────────  0 (物理地址0)
    │                                       │
    │  用户内存                              │
    │  0 → KERNBASE                         │
0x00000000 ─────────────────────────────────── 自由物理内存
```

命名规范（应该是）
pgdir: 页目录表，即pde数组
pgtab: 页表，即pte数组
pde: 页目录，即1024个页表的首地址
pte: 页，即一张页的首地址(ppn+perm)，物理页

walkpgdir():遍历pgdir，寻找到virtual address对应的pte地址（在pgtab中的地址），传入alloc标志，未存在时自动创建对应的页表(即创建pde和pde指向的pte数组)
mappages():将va+size这一块虚拟内存映射到pa为首的物理地址处，什么是映射呢?正常运作时，一个虚拟地址能够对应到一个物理地址（这是因为存在对应的pde/pte)，此时我们有了pgdir和pde，所以映射就是创建对应的pte，还没有创建具体的page，只是存在了这个映射关系了，可能和后面的缺页中断有关

!!!重要!!!
cpu寻址的物理地址空间和虚拟地址空间是一样大的，都和位宽有关系，他们的区别是虚拟地址空间是每个进程独有的，物理地址空间只有一份，所以这里有一层映射，然后实际的物理内存，比如ram，他可能没有4gb,然后物理地址空间也需要进行规划，给mmio, ram，bios等等的，这里又有一层映射
**所以我们说的虚拟地址和物理地址是相对于虚拟地址空间和物理地址空间而言的**

硬件强制固定的：LAPIC（0xFEE00000）、I/O APIC（0xFEC00000）、传统 BIOS 区（低 1MB）等。这些是谁也改不了的。
固件/BIOS 分配的：PCIe 设备的 MMIO 地址、Memory Hole 的位置。这些在不同主板上可能不同，但一旦由固件分配好，操作系统通常只能接受（除非用 pci=realloc 强制重分配）。
操作系统无法随意更改物理地址空间的“硬边界”，但操作系统可以选择如何利用剩余的 RAM 区域（比如哪些地址用作内核堆、哪些用作用户内存）。
**所以，物理地址空间的规划是 “大部分由固件制定，小部分由硬件固定；操作系统是规则的遵守者，而非制定者”。**

```
虚拟地址空间和物理地址空间大小相同（都是4GB），但前者是每个进程的“专属视图”，后者是全局唯一的“硬件总线视图”；页表解决“进程视图→总线视图”的映射，而硬件地址解码解决“总线地址→具体硬件（RAM或设备）”的路由。

[进程A的代码执行 mov eax, [0x1000]]
            ↓
[CPU 发出虚拟地址 0x1000]  ← 软件/程序视角
            ↓
[MMU] 
   - 查当前进程页表（CR3）
   - 权限检查（缺页/保护）
   - PTE中的物理页框号 + 页内偏移
            ↓
[物理地址 0xFE001000]  ← 从此处开始，以下全是硬件电路/总线传输
            ↓
[CPU将物理地址 0xFE001000 放到地址总线上]  ← 物理总线信号
            ↓
[芯片组/内存控制器中的地址译码器]
   - 检查高位地址
   - 区间判断
            ↓
[判断落在 0xFE000000~0xFFFFFFFF（MMIO区域）]
   - 不选中内存控制器
   - 选中对应PCIe/设备总线
            ↓
[请求通过PCIe/设备总线到达目标硬件（如IOAPIC）]
   - 硬件解析剩余的地址作为寄存器偏移
   - 读取或写入设备内部的逻辑寄存器
            ↓
[目标硬件通过数据总线返回数据]
            ↓
[CPU EAX 寄存器获得最终值]
```

| 阶段 | 核心机制 | 操作主体 | 输入 → 输出 |
|------|----------|----------|--------------|
| 虚拟→物理 | 查表（页表） | MMU（硬件） | 虚拟地址 → 物理地址 |
| 物理地址路由 | 硬件译码（地址范围判断） | 芯片组/内存控制器 | 物理地址 → 目标硬件（RAM或设备） |
| 目标硬件内部寻址 | 内部地址译码（行/列或寄存器偏移） | 目标硬件自身的逻辑电路 | 目标硬件地址/偏移 → 内部存储单元/寄存器 |

映射是因为结果不固定，译码是因为结果固定

copyuvm():先安装kernel页映射，然后父进程的用户态内存再虚拟地址空间的分布是[0, sz]，所以将这块虚拟内存复制到新进程中，具体步骤是分配一张页，memmove复制数据, 用mappages建立映射

## sysproc.c/proc.c

allocproc(): 进程的分配（不分配用户态内存，所以p->sz为0）
1. 寻找一个空闲槽位，即pid（proc数组）
2. 分配一个内核栈（一页大小），每个进程独有
3. 在栈底分配一个trapframe（用来存储进程从用户态陷入内核态时的上下文）
4. 压入trapret函数地址（从内核态返回用户态的具体逻辑）
5. 分配一个**进程间**切换上下文（进程切换要处于内核态），初始时的context中的eip指向forkret，当进程被调度时，会执行eip，这样如果是刚创建的进程，就落入forkret执行一些初始化的操作
内存分布
```txt
///////////////////////
// 内存分布
///////////////////////

高地址 (KSTACKSIZE)
+---------------------------+
|                           |
|    空闲空间               |
|                           |
+---------------------------+  ← p->kstack + KSTACKSIZE
|                           |
|    struct trapframe       |  ← p->tf 指向这里（预留）
|    (全部初始化为 0)       |
|                           |
+---------------------------+  ← sp 指向这里（第一次设置）
|                           |
|    trapret 返回地址        |  ← 压入 trapret
|                           |
+---------------------------+  ← sp 指向这里（第二次设置）
|                           |
|    struct context         |  ← p->context 指向这里
|    context->eip = forkret |
|                           |
+---------------------------+  ← 最终 sp 指向这里
低地址

///////////////////////
// 进程切换
///////////////////////

用户态进程 A 运行
    ↓
定时器中断发生（硬件）
    ↓
【用户态 → 内核态】← CPU 自动切换
    ↓
进入中断处理程序（内核态）
    ↓
trap() 识别中断类型
    ↓
如果时间片用完 → 调用 yield()
    ↓
sched() → 选择新进程
    ↓
swtch() → 切换上下文
    ↓
【切换完成，返回用户态】
    ↓
用户态进程 B 运行
```

scheduler():在主循环中遍历进程表，获取一个RUNNING状态的进程，将cpu运行的进程指向他，然后切换到用户页表，此时仍旧能够访问内核的数据（每个用户进程都有相同的内核映射），然后切换context回到用户进程，此时还是处于内核态，需要调用trapret之后才能返回用户态，用户进程时间片用完后再次回到scheduler时要切换到内核页表

fork():使用allocproc分配一个process胚胎（proc数组槽位+一页内核栈），copyuvm复制父进程的虚拟内存（页目录/页表/页），同步proc元信息（直接复制）,p->sz，p->content, p->tf等等，然后复制文件资源，即open file表/inode，设置好状态和返回码即可结束

## kalloc.c
采用空闲链表的方式管理内存，每个节点是一张4096字节大小的页，与lwip的动态内存管理有点相似，每个节点都有管理域(next指针)和数据域(4096 bytes)，空闲时管理域位于数据域的前几个字节，使用时，不需要管理域，数据域全部都可以使用，不需要分配额外的空间给管理域

头插法
kalloc()从空闲链表中头节点取出，更新头节点
kfree()将要释放的page的前几个字节解释为管理域，更新next指针为当前头节点，更新头节点
freerange()对一个范围内的内存，以pagesize为步进长度，依次调用kfree
内存初始时刻即kinit1()/kinit2()时调用freerange来讲一段内存初始化，即将内存规划为一个一个node(4096bytes page)，然后加入到空闲链表中

## syscall.c/syscall.h/usys.S
syscall.h中定义了系统调用号
usys.S中定义了用户到内核的系统的调用接口，即触发T_SYSCALL中断`int $T_SYSCALL`
syscall.c中syscalls数组包含了(系统调用号，系统调用函数)，syscall()函数在用户进程调用系统调用时被执行
以用户调用write为例子 
```c
write(1, "hello", 5);
```
编译器将其编译为
```asm
pushl $5        ; 第三个参数
pushl $hello    ; 第二个参数（字符串地址）
pushl $1        ; 第一个参数（文件描述符）
call  write     ; 调用汇编包装函数 write, call指令会压入pc（指向call的下一条指令）
```
到进入usys.S中的write的时候，**用户栈**中新压入了4个内容，esp指向pc，然后就跳转到usys.S中的write执行，将系统调用号存入eax中，然后触发T_SYSCALL中断，此时有会压入新的内容（应该）到**内核栈**中，esp指向pc（int的下一条指令）， 进入内核态后，**用户态的上下文会被压入trapframe中（即trapframe中的esp还是指向用户栈的栈顶）**，通过中断向量表执行对应的中断处理函数，即syscall()（在syscall.c）中，这时候检查eax（从用户态陷入到内核态，会将环境保存到trapfram中，此时eax存储系统调用号），然后调用syscalls数组对应号的系统调用，并将返回结果回填到eax中
fetchint()和fetchstr()就是将某个地址处的内容转换为某个类型然后传递给调用者
argint()将第n个参数以int（4字节）形式取出（以trapframe中保存的esp为基地址）
argstr()将第n个参数以string（0结尾）形式取出
argptr()将第n个参数以任意形式取出（一个变量=地址+大小，地址我们有了，argptr接收一个大小，就能够取出任意类型的参数了）
进入到系统调用内了，特权级改为内核态了（cpu自动），页表应该是没有切换的，不然无法从用户栈中获取数据，每个用户页表都有相同的内核页映射，所以能够访问内核相关数据

## vectors.S
由`vectors.pl`生成，定义了两部分
1. 中断处理函数，所有的中断全部跳转到alltraps处处理，然后有的中断触发时，cpu会自动压入错误码，有的不会，所以不会压入错误码的需要手动压入，这是为了和trapframe的布局匹配，然后在压入中断号
```asm
.globl vector1
vector1:
  pushl $0
  pushl $1
  jmp alltraps
...
.globl vector8
vector8:
  pushl $8
  jmp alltraps
```
2. 中断向量表，导出vectors给idt的构造使用
```asm
# vector table
.data
.globl vectors
vectors:
  .long vector0
  .long vector1
  .long vector2
  ...
```

## trapasm.S
alltraps显示保存上下文，人为构造一个trapframe（即按照trapframe的布局，将参数压入栈），然后调用trap(struct trapframe *tf)函数再C中处理
trapret从内核态还原到用户态，寄存器弹出

## trap.c

## sysfile.c/file.c

ftable是全局资源，保存有系统所有打开的文件实体，生命周期：系统启动时初始化，一直存在
`struct proc`中的ofile是进程视图，是进程打开的文件实体，生命周期：随进程创建而存在，随进程结束而消亡
打开一个文件时，ftable会分配一个槽位，并将他的ref加一, 然后进程A打开的，就在进程A的ofile处添加指向这个ftable槽位的指针
每次调用open都会新增ftable的文件实体，dup则会增加file的ref

filealloc(): 遍历ftable，寻找到没有使用过的`struct file`，即`file->ref==0`，找到后将ref设置为1，然后返回这个file
filedup(): 将file的ref加一，返回这个file
fileclose(): 将file的ref减1，如果为0就重置type为FD_NONE

## pipe.c

## 文件系统

### 七层模型

xv6 的文件系统采用分层设计，每一层都建立在下层的基础上：

```
┌─────────────────────────────────────────────┐
│  7. 文件描述符层  (File Descriptor)         │  ← 用户态接口 (open/read/write)            sysfile.c/file.c
├─────────────────────────────────────────────┤
│  6. 路径名层      (Pathname)                │  ← 路径解析 (/usr/src/fs.c)                fs.c
├─────────────────────────────────────────────┤
│  5. 目录层        (Directory)               │  ← 目录项 (dentry)                         fs.c
├─────────────────────────────────────────────┤
│  4. 索引节点层    (Inode)                   │  ← 文件元数据 (大小/权限/数据块)           fs.c
├─────────────────────────────────────────────┤
│  3. 日志层        (Logging)                 │  ← 事务/崩溃恢复                           log.c
├─────────────────────────────────────────────┤
│  2. 缓冲区缓存层  (Buffer Cache)            │  ← 缓存磁盘块                              bio.c/buf.h
├─────────────────────────────────────────────┤
│  1. 磁盘层        (Disk)                    │  ← pio 驱动                                ide.c/memide.c
└─────────────────────────────────────────────┘
```

---

### 磁盘布局

文件系统在磁盘上是这样组织的：

```
 xv6.img
┌─────┬─────┬─────┬─────┬─────┬─────┬──────────┬──────────┬──────────┐
│  引导块  │                          block 1 ~ N                               
│  Boot    │                          kernel
│  Block   │                          (ELF格式)                                         
└─────┴─────┴─────┴─────┴─────┴─────┴──────────┴──────────┴──────────┘
   block 0    block 1     block 2..   ...       ...        ...

 fs.img (mkfs.c)
┌─────┬─────┬─────┬─────┬─────┬─────┬──────────┬──────────┬──────────┐
│  引导块  │  超级块  │  日志区  │  inode区 │  位图区  │  数据区  │
│  Boot    │  Super   │  Log     │  Inodes  │  Bitmap  │  Data    │
│  Block   │  Block   │  Blocks  │  Blocks  │  Blocks  │  Blocks  │
└─────┴─────┴─────┴─────┴─────┴─────┴──────────┴──────────┴──────────┘
   block 0    block 1     block 2..   ...       ...        ...
```

| 区域 | 作用 | 关键结构 |
| :--- | :--- | :--- |
| **引导块** | 启动引导程序 | 无 |
| **超级块** | 文件系统元信息 | `struct superblock` |
| **日志区** | 事务日志（崩溃恢复） | `struct logheader` |
| **inode 区** | 存储所有文件的元数据 | `struct dinode` |
| **位图区** | 标记空闲数据块 | `bitmap` |
| **数据区** | 实际文件/目录内容 | `data blocks` |

## buf.h

`struct buf`是内存和磁盘之间交互的代理，是磁盘块的内存拷贝
将常用的磁盘块读入内存，这样读这个磁盘块的速度就会提升许多（磁盘访问->内存访问），对磁盘块进行写的话，可以进行写操作的合并，然后一次写入磁盘（加速
